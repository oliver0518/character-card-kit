---
name: character-card-generator
description: 给一段人物描述/设定文本生成「写实完整人物卡 + Q 版人物卡」双卡。当用户要求「做人物卡」「生成角色设定卡」「给角色出定妆卡」「升级/补 Q 版人物卡」时使用。涵盖考据 → 10 张分件生图 → 配置组装 → 目检全链路。依赖本仓库的 compose_full_card.py / compose_q_card.py（Pillow）。
---

# 人物卡生成流水线（写实完整卡 + Q 版卡）

**交付物（缺一不算完成）**：

| # | 产物 | 说明 |
|---|------|------|
| 1 | 写实完整卡 | `compose_full_card.py` 产出 |
| 2 | Q 版人物卡 | `compose_q_card.py` 产出，**必须独立成卡，不能塞在写实卡里** |
| 3 | 分件源图 ×10 | 写实 5 + Q 版 5（仅返修用，不出片） |
| 4 | 成品目检 | 真看图，不假装看 |

**铁律：一个角色验收后再做下一个，不并行。**

## 第一步 · 考据（先证据后画）

从用户提供的人物描述/设定文档/文稿取证，**逐条带出处**：

- **台词金句**（定性格）→ 挑 1 句最贴人物的进资料栏"口头禅"
- **生活细节**（定道具/微表情，最容易被忽略也最出彩）→ 爱吃什么、随身带什么、紧张时的小动作
- **战力/职业表现**（定动作/装备）→ 武器、技能、标志性动作
- **硬性禁项**：用户明说"不能画成什么"的，逐条抄进 `info_full` 的"硬性"行，并写进后续每张分件的生图提示词

产出：服饰六格名单 + 装备六格名单 + 微表情六个 + 身高体重 + 色板（6 色，hex + RGB）。

## 第二步 · 10 张分件生图

**写实 5 张**（文件名放分件目录）：
`<角色>_主肖像_源图.png`（1024×1024）· `<角色>_三视图_源图.png`（1536×1024，正/侧/背并排）· `<角色>_微表情_源图.png`（1536×1024，六格 3×2）· `<角色>_服饰图鉴_源图.png` · `<角色>_装备图鉴_源图.png`

**Q 版 5 张**：同套命名加 `_Q版`。

### 生图提示词模板

**① 主肖像**：
```
CHARACTER LOCK: 与参考图 image1 同一角色，面部/发型/体型完全一致。
<角色>半身像，<姿势与表情>，<服饰要点>，浅米色纯背景 (240,237,231)，
正面受光，细节清晰，无文字。
```

**② 三视图**（横版 1536×1024）：
```
CHARACTER LOCK: 同 image1 角色。
同一角色三视图：正面、侧面、背面，全身站姿 T-pose 自然站直，
三视图等高并排、间距均匀，同一比例尺，浅米色纯背景 (240,237,231)，
地面可有淡淡阴影，无文字，无标注线。
```

**③ 微表情六格 / ④ 服饰六格 / ⑤ 装备六格**（均横版 1536×1024，3 列×2 行）：
```
CHARACTER LOCK: 同 image1 角色。
3列×2行网格，共6格，每格独立浅米色底 (240,237,231)，格间留白。
每格内容：<逐格写出六格各画什么>。
一律素面，禁画任何文字、字母、数字、标签、名牌。
```
- 微表情六格：同角色头部特写，六种表情（按考据的性格名单）
- 服饰六格：六件服饰/配件的单体特写（源自考据的服饰名单）
- 装备六格：六件装备/随身物的单体特写

**命名口径**：服饰/装备六格在组装时**不叠印逐格名牌**（AI 网格无法像素级对齐，名牌必错位；格上文字还会被下游生图 AI 误读），所以这两组的六格内容只要**排布成 3×2、底色一致**即可，不必死磕对齐。只有**微表情六格**会叠名牌，它必须严格 3×2。

### 生图硬约束

- **锁脸**：`image1` = 该角色现有定妆原图或主肖像；`input_fidelity: "high"`；提示词第一句写 CHARACTER LOCK。
- **⚠️ 挂图超限**：高清分件普遍 5–8MB，可能超生图请求体上限。先用 PIL 缩到 1200px 高质量 JPG（约 300KB）再挂，锁脸不受影响。
- **六格图必须横版 3 列×2 行（1536×1024）**，方图会让名牌错位，必返工。
- 底色统一浅米色 (240,237,231)，否则组装后卡片色带不齐。
- 禁项逐条写进每张分件的提示词。

### 出图后必做目检（看不了图就重出，不得假装目检）

目检 10 张源图：多余肢体/断手、道具上出现文字、多角色撞脸、违反硬性禁项。发现问题**当场重出**，别带到组装阶段。

## 第三步 · 写配置并组装

```bash
# 1) 首次：cp characters.example.py characters.py
#    按考据结果填写 CHARS["<角色>"] 与 Q_CHARS["<角色>"]（字段注释齐全）
# 2) 体检（可选但推荐）：
python check_assets.py
# 3) 组装：
python compose_full_card.py <角色>
python compose_q_card.py   <角色>
```

关键配置项：
- `cm_height`：设定身高（cm），刻度线由脚本按此生成；非人形小型角色（如 20cm 掌上宠物）同样适用。
- `turn_front_x`：三视图源图里**正面视图**所在的 x 像素区间（脚线检测用），写实默认 `(50, 345)`，Q 版按实际排列填（如正面在右侧 `(1060, 1500)`）。
- `turn_wm`：源图右下水印修补策略，地面有渐变阴影时用 `"stretch"` + `turn_patch_span`。

### 已知坑位

| 坑 | 症状 | 解法 |
|----|------|------|
| 微表情名牌错位 | 源图出成方图 | 重出横版 3×2，验证名牌带间距 = 半行高（服饰/装备已不叠名牌，不受影响） |
| 三视图水印补丁露色块 | `corner` 填充在地面渐变上出色块 | `turn_wm="stretch"`，取人物间地面空隙拉伸贴盖 |
| `copy` 补丁复制带进杂物 | 侧面视图串元素 | 同上，用 `stretch` |
| Q 版刻度线失效 | 身高线 top=0 | 用自动背景采样（默认 `ref=None`），别写死背景色 |
| 文书/纸张/招牌/令牌类道具 | AI 几乎必在道具上画文字 | 生图清单里就换成无文字风险道具（铜钱串、布袋、绳结等），或接受后期贴字盖住 |

组装后 **PIL 读尺寸**确认出图成功，并**真目检成品卡**（人类手 / 无多余特效 / 名牌与格子一一对应 / 刻度线贯通 10cm/50cm 分级）。

## 第四步 · 目检成品

两张卡都要看：写实卡（大肖像+资料栏+统一刻度三视图+微表情+服饰六格+装备六格+色板+背景/世界观）、Q 版卡（同上但 Q 版）。检查**微表情**名牌是否与格子一一对应、刻度线 10cm/50cm 分级是否贯通；服饰/装备六格不叠名牌，只需确认排版无错位与水印残留。

## 铁律速查

- **角色设定是铁律，不得自由发挥**——用户给的禁项逐条进提示词。
- **Q 版只能作搞笑嵌格**（单镜 ≤2 秒量级使用），禁整镜 Q 化，禁写实↔Q 对切。
- **锁脸以写实卡为准**，Q 版不得反写设定。
- **一个角色验收后再做下一个**。
